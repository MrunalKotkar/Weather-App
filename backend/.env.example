# ─────────────────────────────────────────────────────────────────────────────
# Backend Environment Variables
# Copy this file to .env for local development
# Set these in Railway → Project → Variables for production
# ─────────────────────────────────────────────────────────────────────────────

# Server
PORT=5000

# ── Local PostgreSQL (used when DATABASE_URL is NOT set) ──────────────────────
DB_HOST=localhost
DB_PORT=5432
DB_NAME=weather_app
DB_USER=postgres
DB_PASSWORD=your_postgres_password

# ── Production PostgreSQL (Railway auto-fills this) ───────────────────────────
# Railway automatically injects DATABASE_URL. You do NOT need to set this manually.
# For other providers (Supabase, Neon, Heroku) paste the full connection string:
# DATABASE_URL=postgresql://user:password@host:port/dbname

# OpenWeatherMap API (https://openweathermap.org/api)
OPENWEATHER_API_KEY=your_openweathermap_api_key_here

# YouTube Data API v3 (https://console.cloud.google.com/)
YOUTUBE_API_KEY=your_youtube_api_key_here

# Frontend URL for CORS (your Vercel URL, e.g. https://weatherwise.vercel.app)
CLIENT_URL=http://localhost:5173
