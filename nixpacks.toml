# Railway Nixpacks config — tells Railway to treat /backend as the app root
[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["cd backend && npm install"]

[phases.build]
cmds = []

[start]
cmd = "node backend/server.js"
